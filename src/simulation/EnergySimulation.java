package simulation;

import java.util.ArrayList;

import energy.EnergySource;
import energy.EnergySource.*;
import models.Weather;
import services.EnergyManagementSystem;
import energy.Solar;

public class EnergySimulation {
	//public static void main(String[] args) {
		//Weather weather = new Weather();
		//ArrayList<EnergySource> eSource = new ArrayList<>(); 
		
		//Solar sl = new Solar(150, weather);
		//Solar s2 = new Solar(100, weather);
		//Solar s3 = new Solar(32, weather);
		//Solar s4 = new Solar(100, weather);
		//eSource.add(sl);
		//eSource.add(s2);
		//eSource.add(s3);
		//eSource.add(s4);
		
		//EnergyManagementSystem EMS = new EnergyManagementSystem(eSource, weather); 
		

		//for (int i = 0; i < eSource.size(); i++) {
			//Thread updateThread = new Thread(eSource.get(i));
			//updateThread.start();
		//}
		
		//Thread thread1 = new Thread(EMS);
		//thread1.start();
		
		
		//try {
			//thread1.join();
		//} catch (InterruptedException e) {
            //e.printStackTrace();
        //}
	//}
}
